# 依赖包，不需要上传（npm install 可以重新生成）
node_modules/

# 构建产物，不需要上传（npm run build 可以重新生成）
dist/

# 本地环境变量（可能含密码，不能上传）
.env
.env.local

# 编辑器配置，每人不同，不需要共享
.DS_Store
.vscode/
.idea/

# Claude Code 本地设置，不需要上传
.claude/
